export default class obstetrician{
    constructor(id, firstName, lastName, email, age, degree) {
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.age = age;
        this.degree = degree;
    }
}